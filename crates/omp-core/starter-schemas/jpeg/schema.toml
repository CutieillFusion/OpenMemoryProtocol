file_type = "jpeg"
mime_patterns = ["image/jpeg", "image/jpg"]

[fields.byte_size]
source = "probe"
probe = "file.size"
type = "int"

[fields.sha256]
source = "probe"
probe = "file.sha256"
type = "string"

[fields.mime]
source = "probe"
probe = "file.mime"
type = "string"

[fields.dimensions]
source = "probe"
probe = "image.dimensions"
type = "object"

[render]
kind = "image"

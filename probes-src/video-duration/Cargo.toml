[package]
name = "video-duration"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "video_duration"
crate-type = ["cdylib"]

[dependencies]
probe-common = { path = "../probe-common" }
probe-mp4 = { path = "../probe-mp4" }
ciborium = { workspace = true }
